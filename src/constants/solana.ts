/**
 * Re-exported Solana constants for convenient access.
 *
 * The canonical source of truth is `src/solana/config.ts`.
 * Import from here (`src/constants/solana`) when you only
 * need the constants without pulling in the full solana module.
 */
export {
  TREASURY_WALLET,
  REVIVE_COST_SOL,
  REWARD_AMOUNT_SOL,
} from "../solana/config";
